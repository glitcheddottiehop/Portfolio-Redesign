import { Plugin } from 'vue';
import { applyPolyfills, defineCustomElements } from '@rehaps/rehaps-web-components/loader';

export const ComponentLibrary: Plugin = {
  async install() {
    applyPolyfills().then(() => {
      defineCustomElements();
    });
  },
};
