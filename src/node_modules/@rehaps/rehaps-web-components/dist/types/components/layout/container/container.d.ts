import { FunctionalComponent } from '../../../stencil-public-runtime';
import { ContainerAnimation } from '../../../interfaces/components/layout/container/types';
export declare class Container {
  host: HTMLElement;
  private intersectionObserver;
  private initialized;
  scrollIntoAnimation: ContainerAnimation;
  scrollOutAnimation: ContainerAnimation;
  animateOpacity: boolean;
  animationDuration: number;
  visibilityThreshold: number;
  onAnimationDurationChange(newValue: number): void;
  isVisible: boolean;
  componentWillLoad(): void;
  componentDidLoad(): void;
  private initIntersectionObserver;
  private handleIntersection;
  private removeIntersectionObserver;
  private setAnimationDuration;
  private get hasScrollIntoAnimation();
  private get hasScrollOutAnimation();
  render(): FunctionalComponent;
}
