export function simulateMouseEvent(element, eventName, x, y, composedPath = null) {
  const event = new MouseEvent(eventName, {
    'view': window,
    'bubbles': true,
    'cancelable': true,
    'clientX': x,
    'clientY': y,
    'screenX': x,
    'screenY': y,
  });
  event.composedPath = function () {
    return composedPath || [element];
  };
  element.dispatchEvent(event);
}
//# sourceMappingURL=mouse.js.map
