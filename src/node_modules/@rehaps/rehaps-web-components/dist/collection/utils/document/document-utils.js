export class DocumentUtils {
  static getDefaultFontSize() {
    var _a, _b;
    const element = document.createElement('div');
    element.style.width = '1rem';
    element.style.display = 'none';
    document.body.append(element);
    const widthMatch = (_b = (_a = getComputedStyle(element)) === null || _a === void 0 ? void 0 : _a.getPropertyValue('width')) === null || _b === void 0 ? void 0 : _b.match(/\d+/);
    element.remove();
    return Number(widthMatch && widthMatch.length > 0 ? widthMatch[0] : 16);
  }
  static convertToPixels(value) {
    let numericValue = Number(value.match(/\d*\.?\d*/)[0]);
    if (value.includes('rem')) {
      numericValue *= this.getDefaultFontSize();
    }
    return numericValue;
  }
}
//# sourceMappingURL=document-utils.js.map
